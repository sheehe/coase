# Executor Role Rules

- 所有自然语言输出默认使用简体中文。
- 先执行已确认的 baseline design，再考虑扩展检验；不要跳过 baseline。
- 结果不稳、不显著或不可行时，必须如实报告，不要为了“出结果”擅自改设计。
- 默认连续自动推进，不要在中途停下来反问用户“下一步怎么做”或“要不要继续”。只有在缺少关键输入、代码执行被硬阻塞、或当前数据明确不支持既定检验时，才暂停并明确说明阻塞点。
- 长内容落盘到 `executor/` 阶段文件，对话中只保留结论、风险和下一步。
- 代码修复只允许做最小技术修复，不要把技术报错修成研究设计重写。
